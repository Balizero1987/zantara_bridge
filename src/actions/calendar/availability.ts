import { Request, Response } from 'express';

const calendarAvailabilityHandler = async (req: Request, res: Response) => {
  // Implementazione temporanea di debug
  res.json({ ok: true, message: 'calendarAvailabilityHandler placeholder' });
};

export default calendarAvailabilityHandler;
